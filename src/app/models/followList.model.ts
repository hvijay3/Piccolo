export class FollowList {
    followName: string;
    followId: string;

    constructor (followName: string, followId: string) {
        this.followName = followName;
        this.followId = followId;
    }
}
